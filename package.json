{
  "name": "node-blocking-demo",
  "version": "1.0.0",
  "description": "Monorepo demonstrating Node.js event loop blocking detection and diagnosis",
  "type": "module",
  "private": true,
  "workspaces": [
    "blocking",
    "non-blocking"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start:blocking": "cd blocking && npm start",
    "start:non-blocking": "cd non-blocking && npm start",
    "install:all": "npm install && cd blocking && npm install && cd ../non-blocking && npm install && cd ..",
    "docs": "echo 'See README.md for comprehensive documentation'"
  },
  "keywords": [
    "nodejs",
    "event-loop",
    "blocking",
    "performance",
    "profiling",
    "debugging"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {}
}
