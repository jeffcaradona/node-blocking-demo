{
  "name": "node-blocking-demo",
  "version": "1.0.0",
  "description": "Monorepo demonstrating Node.js 25.x event loop blocking detection and diagnosis",
  "private": true,
  "workspaces": [
    "blocking",
    "non-blocking"
  ],
  "engines": {
    "node": ">=25.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start:blocking": "cd blocking && npm start",
    "start:non-blocking": "cd non-blocking && npm start",
    "install:all": "npm install && cd blocking && npm install && cd ../non-blocking && npm install && cd ..",
    "docs": "echo 'See README.md for comprehensive documentation'"
  },
  "keywords": [
    "nodejs",
    "event-loop",
    "blocking",
    "performance",
    "profiling",
    "debugging"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {}
}
